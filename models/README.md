# Models

Tutaj umieszczaj wytrenowane wagi/checkpointy (gdy używasz inferencji bez ponownego uczenia).

Pliki w tym katalogu nie są commitowane (patrz `.gitignore`). Na Kaggle można wgrać wagi jako osobny dataset i dołączyć go w notatniku.
